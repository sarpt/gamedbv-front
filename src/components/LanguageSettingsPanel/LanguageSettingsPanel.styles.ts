import styled from 'styled-components';

import FormControlLabel from '@material-ui/core/FormControlLabel';

export const SelectLabel = styled(FormControlLabel)`
  margin-left: 0px;

  & > .MuiFormControlLabel-label {
    margin-left: 0.5rem;
  }
`;
